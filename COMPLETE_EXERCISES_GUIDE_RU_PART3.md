# 🎓 ПОЛНЫЙ РАЗБОР - ЧАСТЬ 3 (ФИНАЛ)

## Ex_06 - Ex_08: Матрицы, Строки, Структуры

---

## Ex_06: Матрицы (4 задания)

### 🎯 Что ты узнаешь:
- Двумерные массивы
- Умножение матриц
- Swap строк в матрице
- Поворот матрицы на 90°
- Спиральная матрица

---

### 📝 Ex_06_01: Умножение матриц

#### 🎯 Что такое матрица?

**Матрица** - это прямоугольная таблица чисел.

```
Матрица 3×2 (3 строки, 2 столбца):
[1  2]
[3  4]
[5  6]
```

#### 📐 Формула умножения матриц

**A (m×n) × B (n×p) = C (m×p)**

```
C[i][j] = A[i][0]*B[0][j] + A[i][1]*B[1][j] + ... + A[i][n-1]*B[n-1][j]
```

#### Пример

```
A (2×2):     B (2×2):     C = A×B (2×2):
[1 2]        [5 6]        [1*5+2*7  1*6+2*8]   [19 22]
[3 4]    ×   [7 8]    =   [3*5+4*7  3*6+4*8] = [43 50]
```

**Подробно C[0][0]:**
```
C[0][0] = A[0][0]*B[0][0] + A[0][1]*B[1][0]
        = 1*5 + 2*7
        = 5 + 14
        = 19
```

#### Код (основная идея)

```c
// Умножение матриц A(m×n) и B(n×p)
for (int i = 0; i < m; i++) {           // Строки A
    for (int j = 0; j < p; j++) {       // Столбцы B
        C[i][j] = 0;
        for (int k = 0; k < n; k++) {   // Суммирование
            C[i][j] += A[i][k] * B[k][j];
        }
    }
}
```

#### 🎮 Аналогия

Представь два плейлиста:
- Плейлист A: какие песни в какие дни (дни × песни)
- Плейлист B: сколько раз каждую песню слушаешь (песни × настроение)
- Результат C: сколько музыки в каждый день для каждого настроения!

#### ⚠️ Важно!

Можно умножать только если:
- Количество столбцов в A = количество строк в B

```
A (3×2) × B (2×4) = C (3×4)  ✅ Можно
A (3×2) × B (3×4) = ?        ❌ Нельзя!
```

#### 🎥 Видео по теме

1. [Умножение матриц - визуализация](https://www.youtube.com/watch?v=OAh573i_qn8)
2. [Матрицы в программировании](https://www.youtube.com/watch?v=GH_l8wWZhds)
3. [3Blue1Brown - Линейная алгебра](https://www.youtube.com/watch?v=kYB8IZa5AuE) - ЛУЧШЕЕ объяснение!

---

### 📝 Ex_06_02: Swap строк (min и max)

#### Концепция

1. Найти строку с минимальным элементом
2. Найти строку с максимальным элементом
3. Поменять эти строки местами

#### Пример

```
Исходная матрица:
[5  2  8]
[1  9  3]  ← Минимум (1)
[7  4  6]

После swap:
[5  2  8]
[7  4  6]  ← Были внизу
[1  9  3]  ← Были в середине
```

#### Алгоритм

```
1. min_row = 0, max_row = 0
2. min_val = arr[0][0], max_val = arr[0][0]

3. FOR каждой строки i:
   4. FOR каждого элемента j в строке:
      5. ЕСЛИ arr[i][j] < min_val:
         6. min_val = arr[i][j]
         7. min_row = i
      8. ЕСЛИ arr[i][j] > max_val:
         9. max_val = arr[i][j]
         10. max_row = i

11. Swap строки min_row и max_row
```

#### Код swap строк

```c
void swap_rows(int **matrix, int row1, int row2, int cols) {
    for (int j = 0; j < cols; j++) {
        int temp = matrix[row1][j];
        matrix[row1][j] = matrix[row2][j];
        matrix[row2][j] = temp;
    }
}
```

#### 🎮 Аналогия

У тебя полки с книгами:
- Находишь полку с самой тонкой книгой
- Находишь полку с самой толстой книгой
- Меняешь эти полки местами!

#### 🎥 Видео по теме

1. [Swap алгоритм](https://www.youtube.com/watch?v=RIUJ2yng7Ws)
2. [Поиск min/max в матрице](https://www.youtube.com/watch?v=F3PQhLPF8e0)

---

### 📝 Ex_06_03: Поворот матрицы на 90° по часовой

#### Концепция

Повернуть матрицу m×n → n×m

```
Было (3×4):        Стало (4×3):
[1  2  3  4]       [9  5  1]
[5  6  7  8]   →   [10 6  2]
[9  10 11 12]      [11 7  3]
                   [12 8  4]
```

#### Формула поворота

```
new[j][m-1-i] = old[i][j]
```

Где:
- `i` - строка в старой матрице
- `j` - столбец в старой матрице
- `m` - количество строк в старой

#### Визуализация

```
old[0][0] → new[0][2]  (1 переходит вправо вверх)
old[0][1] → new[1][2]  (2 переходит вправо вверх)
old[1][0] → new[0][1]  (5 переходит вправо в середину)
...
```

#### Код

```c
// Поворот матрицы m×n
int **rotated = malloc(n * sizeof(int*));
for (int i = 0; i < n; i++) {
    rotated[i] = malloc(m * sizeof(int));
}

for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        rotated[j][m - 1 - i] = original[i][j];
    }
}
```

#### 🎮 Аналогия

Поворачиваешь фотографию:
- Левый верхний угол → правый верхний
- Правый верхний → правый нижний
- И так далее!

#### 🎥 Видео по теме

1. [Поворот матрицы - визуализация](https://www.youtube.com/watch?v=SA867FvqHrM)
2. [Matrix rotation алгоритм](https://www.youtube.com/watch?v=fMSJSS7eO1w)

---

### 📝 Ex_06_04: Спиральная матрица

#### Концепция

Заполнить матрицу n×n числами по спирали от краёв к центру.

#### Пример n=4

```
[1  2  3  4]
[12 13 14 5]
[11 16 15 6]
[10 9  8  7]
```

#### Алгоритм

```
1. Начать с числа 1
2. Идти вправо по верхней строке
3. Идти вниз по правому столбцу
4. Идти влево по нижней строке
5. Идти вверх по левому столбцу
6. Повторить для внутреннего слоя
```

#### Визуализация слоёв

```
Слой 0 (внешний):
→ → → ↓
↑     ↓
↑ ← ← ←

Слой 1 (внутренний):
  → ↓
  ↑ ←
```

#### Код (основная идея)

```c
int num = 1;
int top = 0, bottom = n-1, left = 0, right = n-1;

while (top <= bottom && left <= right) {
    // Вправо по верху
    for (int i = left; i <= right; i++)
        matrix[top][i] = num++;
    top++;

    // Вниз по правому
    for (int i = top; i <= bottom; i++)
        matrix[i][right] = num++;
    right--;

    // Влево по низу
    for (int i = right; i >= left; i--)
        matrix[bottom][i] = num++;
    bottom--;

    // Вверх по левому
    for (int i = bottom; i >= top; i--)
        matrix[i][left] = num++;
    left++;
}
```

#### 🎮 Аналогия

Рисуешь улитку:
- Начинаешь с края листа
- Идёшь по периметру
- Постепенно заворачиваешься к центру
- Спираль!

#### 🎥 Видео по теме

1. [Spiral Matrix алгоритм](https://www.youtube.com/watch?v=siKFOI8PNKM)
2. [Leetcode Spiral Matrix](https://www.youtube.com/watch?v=BJnMZNwUk1M)

---

## Ex_07: Работа со строками (4 задания)

### 🎯 Что ты узнаешь:
- Удаление лишних пробелов
- Удаление подстроки
- Проверка на палиндром
- Нормализация строк

---

### 📝 Ex_07_01: Удалить лишние пробелы

#### Задача

Заменить несколько пробелов подряд на один.

**Вход:** `"Hello    world     test"`
**Выход:** `"Hello world test"`

#### Алгоритм

```
1. previous_space = false
2. FOR каждого символа:
   3. ЕСЛИ символ пробел:
      4. ЕСЛИ previous_space == false:
         5. Добавить пробел
         6. previous_space = true
   7. ИНАЧЕ:
      8. Добавить символ
      9. previous_space = false
```

#### Код

```c
void remove_extra_spaces(char *str) {
    int i = 0, j = 0;
    int prev_space = 0;

    while (str[i]) {
        if (str[i] == ' ') {
            if (!prev_space) {
                str[j++] = ' ';
                prev_space = 1;
            }
        } else {
            str[j++] = str[i];
            prev_space = 0;
        }
        i++;
    }
    str[j] = '\0';
}
```

#### 🎮 Аналогия

Убираешь дубликаты людей в очереди:
- Человек, пустое место, человек → ОК
- Человек, пусто, пусто, пусто, человек → Оставь одно пусто!

#### 🎥 Видео по теме

1. [Строки в C](https://www.youtube.com/watch?v=PeHKWf8z-i0)
2. [Обработка строк](https://www.youtube.com/watch?v=GiPaL_QSe60)

---

### 📝 Ex_07_02: Удалить последнее вхождение подстроки

#### Задача

Найти и удалить **последнее** вхождение подстроки.

**Вход:** строка `"This is mytext"`, подстрока `"my"`
**Выход:** `"This is text"`

#### Алгоритм

```
1. Найти последнее вхождение подстроки
2. Если не найдено: вернуть исходную строку
3. Если найдено:
   4. Удалить найденную подстроку
   5. Сдвинуть оставшуюся часть влево
```

#### Функция поиска с конца

```c
int find_last(char *text, char *sub) {
    int text_len = strlen(text);
    int sub_len = strlen(sub);

    for (int i = text_len - sub_len; i >= 0; i--) {
        int found = 1;
        for (int j = 0; j < sub_len; j++) {
            if (text[i + j] != sub[j]) {
                found = 0;
                break;
            }
        }
        if (found) return i;  // Позиция найдена!
    }
    return -1;  // Не найдено
}
```

#### Удаление подстроки

```c
void remove_substring(char *text, int pos, int len) {
    // Сдвигаем всё что после подстроки
    for (int i = pos; text[i + len]; i++) {
        text[i] = text[i + len];
    }
    text[strlen(text) - len] = '\0';
}
```

#### 🎮 Аналогия

Ищешь последнюю ошибку в тексте и стираешь её:
- Читаешь с конца
- Нашёл ошибку → стираешь
- Остальное сдвигаешь

#### 🎥 Видео по теме

1. [Поиск в строке](https://www.youtube.com/watch?v=7pPUz6FQnO4)
2. [Работа с подстроками](https://www.youtube.com/watch?v=sCpC-D5EvVY)

---

### 📝 Ex_07_03: Проверка на палиндром

#### Задача

Проверить, является ли строка палиндромом (читается одинаково слева направо и справа налево).

**Примеры:**
- `"madam"` → YES (палиндром)
- `"racecar"` → YES
- `"hello"` → NO
- `"А роза упала на лапу Азора"` → YES (игнорируя пробелы и регистр)

#### Алгоритм

```
1. Удалить пробелы из строки
2. Привести к нижнему регистру
3. Сравнить строку с её реверсом
4. Если равны → палиндром
```

#### Код

```c
int is_palindrome(char *str) {
    int len = strlen(str);
    int left = 0, right = len - 1;

    while (left < right) {
        // Пропускаем пробелы слева
        while (str[left] == ' ') left++;
        // Пропускаем пробелы справа
        while (str[right] == ' ') right--;

        // Сравниваем символы (игнорируя регистр)
        if (tolower(str[left]) != tolower(str[right])) {
            return 0;  // Не палиндром
        }

        left++;
        right--;
    }

    return 1;  // Палиндром!
}
```

#### 📊 Визуализация

```
"racecar"
 ↓     ↑
 r = r  ✓
  ↓   ↑
  a = a  ✓
   ↓ ↑
   c = c  ✓
    ↓
   (встретились в центре)

Результат: Палиндром!
```

#### 🎮 Аналогия

Проверяешь, симметричен ли мост:
- Идёшь с двух сторон
- Сравниваешь каждую часть
- Если всё одинаково → симметричен!

#### 🎥 Видео по теме

1. [Палиндромы - алгоритм](https://www.youtube.com/watch?v=fAwKe4YqMNE)
2. [Two pointers техника](https://www.youtube.com/watch?v=On03HWe2tZM)

---

### 📝 Ex_07_04: Нормализация строки

#### Задача

Удалить все лишние пробелы (в начале, в конце, и множественные в середине).

**Вход:** `"   test   sample   test   "`
**Выход:** `"test sample test"`

#### Алгоритм

```
1. Пропустить пробелы в начале
2. Копировать слова
3. Между словами ставить ровно 1 пробел
4. Убрать пробелы в конце
```

#### Код

```c
void normalize(char *str) {
    int i = 0, j = 0;

    // Пропускаем начальные пробелы
    while (str[i] == ' ' || str[i] == '\t') i++;

    while (str[i]) {
        // Копируем символ если не пробел
        if (str[i] != ' ' && str[i] != '\t') {
            str[j++] = str[i++];
        }
        // Если пробел - добавляем один и пропускаем остальные
        else {
            str[j++] = ' ';
            while (str[i] == ' ' || str[i] == '\t') i++;
        }
    }

    // Убираем последний пробел если есть
    if (j > 0 && str[j-1] == ' ') j--;

    str[j] = '\0';
}
```

#### 🎮 Аналогия

Наводишь порядок в гараже:
- Убираешь мусор с краёв
- Расставляешь вещи с равными промежутками
- Чистота!

#### 🎥 Видео по теме

1. [Обработка строк - практика](https://www.youtube.com/watch?v=IrGjyfBC-u0)
2. [Trim и normalize](https://www.youtube.com/watch?v=OHcgM8OcdvI)

---

## Ex_08: Структуры данных (2 задания)

### 🎯 Что ты узнаешь:
- struct в C
- Массивы структур
- Сортировка структур
- Работа с записями

---

### 📝 Ex_08_01: Студенты с оценкой выше среднего

#### Задача

Дан список студентов (имя + оценка). Вывести тех, у кого оценка выше среднего.

#### 🆕 Структуры в C

```c
struct Student {
    char name[256];
    int grade;
};
```

**Что это?**
Структура = контейнер для разных типов данных.

**Аналогия:** Карточка студента:
- Имя (строка)
- Оценка (число)
- Возраст (число)
- ...

#### Создание массива структур

```c
struct Student students[100];  // Статический массив

// Или динамический:
struct Student *students = malloc(n * sizeof(struct Student));
```

#### Доступ к полям

```c
struct Student s;
strcpy(s.name, "Ivan");  // Имя
s.grade = 85;            // Оценка

printf("%s: %d\n", s.name, s.grade);
// Вывод: Ivan: 85
```

#### Алгоритм задачи

```
1. Считать n (количество студентов)
2. Выделить память для n студентов
3. FOR i от 0 до n-1:
   4. Считать имя и оценку студента i
5. Посчитать среднюю оценку
6. FOR i от 0 до n-1:
   7. ЕСЛИ оценка > среднего:
      8. Вывести имя
```

#### Код

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_NAME 256

struct Student {
    char name[MAX_NAME];
    int grade;
};

int main() {
    int n;
    scanf("%d", &n);
    getchar();  // Съесть \n

    struct Student *students = malloc(n * sizeof(struct Student));

    // Читаем студентов
    for (int i = 0; i < n; i++) {
        fgets(students[i].name, MAX_NAME, stdin);
        // Убираем \n в конце
        students[i].name[strcspn(students[i].name, "\n")] = '\0';

        scanf("%d", &students[i].grade);
        getchar();
    }

    // Средняя оценка
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += students[i].grade;
    }
    int avg = sum / n;

    // Вывод выше среднего
    for (int i = 0; i < n; i++) {
        if (students[i].grade > avg) {
            printf("%s,", students[i].name);
        }
    }

    free(students);
    return 0;
}
```

#### 📊 Пример

**Ввод:**
```
3
Alice
90
Bob
70
Charlie
80
```

**Средняя:** (90 + 70 + 80) / 3 = 80

**Вывод:** `Alice,` (90 > 80)

#### 🎮 Аналогия

У тебя список друзей с их ростом:
- Считаешь средний рост
- Выводишь тех, кто выше среднего!

#### 🎥 Видео по теме

1. [Структуры в C](https://www.youtube.com/watch?v=wkqnAHcApv4)
2. [Массивы структур](https://www.youtube.com/watch?v=CpE_tfm_g1c)
3. [Работа со структурами - практика](https://www.youtube.com/watch?v=49dYaR3sFzc)

---

### 📝 Ex_08_02: Сортировка студентов по баллам

#### Задача

Отсортировать студентов по оценкам (по убыванию - от большего к меньшему).

#### Алгоритм пузырьковой сортировки

```
FOR i от 0 до n-2:
    FOR j от 0 до n-i-2:
        ЕСЛИ arr[j].grade < arr[j+1].grade:
            Swap arr[j] и arr[j+1]
```

#### Визуализация

```
Начало: [70, 90, 80]
         ↓   ↓
Сравниваем 70 и 90 → Swap → [90, 70, 80]
                           ↓   ↓
Сравниваем 70 и 80 → Swap → [90, 80, 70]

Результат: [90, 80, 70] ✓
```

#### Код

```c
void sort_students(struct Student *arr, int n) {
    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j].grade < arr[j+1].grade) {
                // Swap
                struct Student temp = arr[j];
                arr[j] = arr[j+1];
                arr[j+1] = temp;
            }
        }
    }
}

// Использование:
sort_students(students, n);

// Вывод
for (int i = 0; i < n; i++) {
    printf("%s: %d\n", students[i].name, students[i].grade);
}
```

#### 📊 Пример

**До сортировки:**
```
Alice: 70
Bob: 90
Charlie: 80
```

**После сортировки:**
```
Bob: 90
Charlie: 80
Alice: 70
```

#### 🎮 Аналогия

Выстраиваешь людей по росту:
- Сравниваешь соседей
- Меняешь местами если не по порядку
- Повторяешь пока не будет порядок!

#### 🆕 Другие алгоритмы сортировки

| Алгоритм | Сложность | Когда использовать |
|----------|-----------|-------------------|
| Bubble Sort | O(n²) | Обучение, малые данные |
| Quick Sort | O(n log n) | Общий случай |
| Merge Sort | O(n log n) | Гарантированная скорость |
| Insertion Sort | O(n²) | Почти отсортированные |

#### 🎥 Видео по теме

1. [Bubble Sort визуализация](https://www.youtube.com/watch?v=xli_FI7CuzA)
2. [Sorting Algorithms Comparison](https://www.youtube.com/watch?v=kPRA0W1kECg)
3. [Quick Sort explained](https://www.youtube.com/watch?v=Hoixgm4-P4M)

---

## 🎉 ПОЗДРАВЛЯЮ! ТЫ ПРОШЛА ВСЁ!

### 📊 Финальная статистика

| Уровень | Упражнения | Темы |
|---------|-----------|------|
| Ex_01 | 6 | Арифметика, ввод/вывод |
| Ex_02 | 5 | Форматирование |
| Ex_03 | 5 | Математика, логика |
| Ex_04 | 4 | Циклы, последовательности |
| Ex_05 | 5 | Динамическая память |
| Ex_06 | 4 | Матрицы |
| Ex_07 | 4 | Строки |
| Ex_08 | 2 | Структуры |
| **ВСЕГО** | **35** | **8 категорий** |

---

## 🎓 Что ты теперь знаешь?

### ✅ Базовые концепции:
- Типы данных (int, float, double, char)
- Переменные и константы
- Операторы (арифметические, логические, сравнения)
- Ввод/вывод (scanf, printf)

### ✅ Контроль потока:
- Условия (if, else, switch)
- Циклы (for, while, do-while)
- Логические выражения (&&, ||, !)

### ✅ Функции:
- Объявление и вызов
- Параметры и возвращаемые значения
- Рекурсия (factorial)

### ✅ Массивы:
- Одномерные массивы
- Двумерные массивы (матрицы)
- Динамические массивы

### ✅ Память:
- malloc/free
- realloc
- Указатели
- Утечки памяти

### ✅ Строки:
- Символьные массивы
- Обработка строк
- Поиск и замена

### ✅ Структуры:
- Определение struct
- Массивы структур
- Работа с полями

---

## 🚀 Следующие шаги

### 1. Практика (ОБЯЗАТЕЛЬНО!)

Решай задачи:
- [Codewars](https://www.codewars.com/) - 30 минут в день
- [LeetCode](https://leetcode.com/) - начни с Easy
- [HackerRank](https://www.hackerrank.com/) - C трек

### 2. Углублённое изучение

📚 **Книги:**
- "C Programming Language" - Керниган и Ритчи (библия!)
- "C Primer Plus" - Стивен Прата
- "Head First C" - Дэвид Гриффитс

🎥 **Курсы:**
- CS50 от Гарварда (ОБЯЗАТЕЛЬНО!)
- C Programming Tutorial for Beginners (freeCodeCamp)
- ITVDN - Профессиональный C

### 3. Проекты

Создай что-то своё:
- Калькулятор
- Игра "Угадай число"
- Менеджер задач
- Простая база данных

### 4. Подготовка к экзаменам

- Решай задачи с таймером (3 часа на экзамен)
- Используй norminette локально
- Тренируй память (меньше подглядываний в Google)
- Пиши код руками, а не копируй!

---

## 💪 Мотивация

> "Программирование - это супер-сила. Ты можешь создать ЧТО УГОДНО из ничего, используя только мозг и клавиатуру." - Марк Цукерберг

### Правило 10,000 часов

- 100 часов → базовые навыки ✅ (ТЫ ЗДЕСЬ!)
- 1,000 часов → уверенный разработчик
- 10,000 часов → эксперт

**Ты уже на пути! Продолжай!** 🚀

---

## 📞 Полезные ресурсы

### Документация:
- [cppreference.com](https://en.cppreference.com/w/c)
- [C Manual Pages](https://man7.org/linux/man-pages/)

### Сообщества:
- [Stack Overflow](https://stackoverflow.com/questions/tagged/c)
- [Reddit r/C_Programming](https://www.reddit.com/r/C_Programming/)
- School 21 Peer Community

### Инструменты:
- **Компилятор:** clang (School 21 standard)
- **Отладчик:** gdb, lldb
- **Проверка памяти:** valgrind, leaks
- **Стиль кода:** norminette

---

## 🎯 Чеклист перед экзаменом

```
□ Умею работать с scanf/printf
□ Понимаю указатели
□ Знаю malloc/free
□ Умею работать с массивами
□ Знаю основы строк
□ Понимаю циклы
□ Умею работать со структурами
□ Знаю как проверить ввод
□ Умею отлаживать код
□ Практиковался с таймером
□ Установил norminette
□ Знаю флаги компиляции
□ Готов к экзамену! 💪
```

---

## 🏆 Финальный совет

**Не бойся ошибок!**

Каждая ошибка = опыт. Лучшие программисты сделали миллионы ошибок прежде чем стали экспертами.

**Формула успеха:**
```c
while (не_сдался) {
    практика++;
    ошибки++;
    опыт++;
}
success = true;
```

**Удачи на экзаменах School 21!** 🎓🚀

---

**Версия:** 1.0 (Часть 3 из 3 - ФИНАЛ)
**Дата:** 2025-11-17
**Прогресс:** 35/35 упражнений (100%) ✅

**P.S.** Если этот гайд помог - поделись с друзьями из School 21! Let's code together! 💻
