# 🎓 Student Workflow Guide - School 21 Verter Auto-Checker

Это руководство объясняет, как использовать автоматическую систему проверки заданий School 21.

## 📋 Как это работает?

Система работает как **Verter** в School 21:
1. Вы делаете задание локально
2. Пушите код в репозиторий на ветку `develop`
3. Автоматически запускается проверка (через GitHub Actions)
4. Получаете детальный отчет с результатами

---

## 🚀 Пошаговая инструкция

### Шаг 1: Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd exams_21
```

### Шаг 2: Переключитесь на ветку develop

```bash
git checkout develop
# Если ветки нет, создайте её:
git checkout -b develop
```

### Шаг 3: Читайте задание

Все задания находятся в папке `tasks/`:

```bash
cat tasks/first_word.txt
```

Пример задания:
```
Assignment name  : first_word
Expected files   : first_word.c
Allowed functions: write
--------------------------------------------------------------------------------

Write a program that takes a string and displays its first word...
```

### Шаг 4: Создайте решение

Создайте файл в соответствующей папке `submissions/`:

```bash
# Для Level 1 заданий:
nano submissions/level_1/first_word.c

# Для Level 2 заданий:
nano submissions/level_2/ft_atoi.c

# И так далее...
```

**Важно:** Имя файла должно точно совпадать с названием задания!

#### Пример решения:

```c
// submissions/level_1/first_word.c
#include <unistd.h>

int main(int argc, char **argv)
{
    int i = 0;

    if (argc == 2)
    {
        while (argv[1][i] == ' ' || argv[1][i] == '\t')
            i++;
        while (argv[1][i] && argv[1][i] != ' ' && argv[1][i] != '\t')
        {
            write(1, &argv[1][i], 1);
            i++;
        }
    }
    write(1, "\n", 1);
    return (0);
}
```

### Шаг 5: Проверьте локально (опционально)

Перед пушем можете проверить локально:

```bash
./test_runner.sh
```

Вы увидите:
```
╔═══════════════════════════════════════════════════════╗
║       🎓 School 21 Verter Auto-Checker 🎓        ║
╚═══════════════════════════════════════════════════════╝

Testing: first_word [student]
  Source: submissions/level_1/first_word.c
  ✓ PASSED
```

### Шаг 6: Закоммитьте изменения

```bash
# Добавить файл
git add submissions/level_1/first_word.c

# Создать коммит
git commit -m "Add first_word solution"
```

### Шаг 7: Запушьте на ветку develop

```bash
git push origin develop
```

### Шаг 8: Проверьте результаты

1. Перейдите на GitHub в ваш репозиторий
2. Откройте вкладку **Actions**
3. Найдите свой последний коммит
4. Посмотрите результаты проверки

---

## 📊 Интерпретация результатов

### ✅ Тест прошел успешно

```
Testing: first_word [student]
  Source: submissions/level_1/first_word.c
  ✓ PASSED
```

Отлично! Задание выполнено правильно.

### ❌ Тест провален

```
Testing: first_word [student]
  Source: submissions/level_1/first_word.c
  ✗ FAILED
  ┌─ Error Details:
  │ Test 1 failed: expected 'hello', got 'helo'
  └────────────────
```

Что означает:
- Ваш код не прошел тест
- В скобках показана детальная ошибка
- Нужно исправить логику программы

### ⊘ Тест пропущен

```
⊘ SKIPPED: first_word (file not found)
  Expected file: submissions/level_1/first_word.c
```

Что означает:
- Файл не найден
- Проверьте путь и имя файла
- Убедитесь что файл закоммичен

---

## 📁 Структура проекта

```
exams_21/
├── submissions/              # ← Ваши решения здесь!
│   ├── level_1/             # Задания Level 1
│   │   ├── first_word.c
│   │   ├── fizzbuzz.c
│   │   └── ...
│   ├── level_2/             # Задания Level 2
│   │   ├── ft_atoi.c
│   │   └── ...
│   ├── level_3/             # Задания Level 3
│   └── level_4/             # Задания Level 4
│
├── tasks/                   # Описания заданий (только чтение)
│   ├── first_word.txt
│   ├── fizzbuzz.txt
│   └── ...
│
├── tests/                   # Тестовые скрипты (не трогать)
├── answers/                 # Референсные решения (для примера)
└── test_runner.sh          # Скрипт запуска тестов
```

---

## 🎯 Список заданий по уровням

### Level 1 (базовый)
- first_word
- fizzbuzz
- ft_strcpy
- ft_strlen
- ft_swap
- ft_putstr
- repeat_alpha
- rev_print
- rot_13
- rotone
- search_and_replace
- ulstr

### Level 2 (средний)
- alpha_mirror
- do_op
- ft_atoi
- ft_strcmp
- ft_strcspn
- ft_strdup
- ft_strpbrk
- ft_strrev
- ft_strspn
- inter
- is_power_of_2
- last_word
- max
- print_bits
- reverse_bits
- snake_to_camel
- swap_bits
- union
- wdmatch

### Level 3 (продвинутый)
- add_prime_sum
- epur_str
- expand_str
- ft_atoi_base
- ft_list_size
- ft_range
- ft_rrange
- hidenp
- lcm
- paramsum
- pgcd
- print_hex
- rstr_capitalizer
- str_capitalizer
- tab_mult

### Level 4 (эксперт)
- flood_fill
- fprime
- ft_itoa
- ft_list_foreach
- ft_list_remove_if
- ft_split
- rev_wstr
- rostring
- sort_int_tab
- sort_list

---

## 💡 Советы и рекомендации

### ✅ DO (Делайте так):
- Читайте задание внимательно
- Тестируйте локально перед пушем
- Пишите чистый код (norminette style)
- Коммитьте по одному заданию за раз
- Используйте осмысленные сообщения коммитов

### ❌ DON'T (Не делайте так):
- Не изменяйте файлы в `tests/` или `answers/`
- Не пушьте в `main` напрямую
- Не копируйте решения из `answers/` (это для обучения!)
- Не игнорируйте ошибки компиляции

---

## 🐛 Типичные ошибки

### 1. Ошибка компиляции

```
Compilation failed:
error: 'i' undeclared (first use in this function)
```

**Решение:** Объявите все переменные в начале функции (C89 style).

### 2. Неправильный вывод

```
Test 1 failed: expected 'hello', got 'hello '
```

**Решение:** Проверьте пробелы, переносы строк и другие символы вывода.

### 3. Утечки памяти (для функций с malloc)

```
Memory leak detected
```

**Решение:** Не забывайте `free()` для всех `malloc()`.

### 4. Файл не найден

```
⊘ SKIPPED: first_word (file not found)
```

**Решение:**
- Проверьте путь: `submissions/level_1/first_word.c`
- Проверьте имя файла (должно точно совпадать)
- Убедитесь что файл закоммичен: `git add submissions/level_1/first_word.c`

---

## 🔍 Как посмотреть детальные результаты?

### В GitHub Actions:

1. Перейдите в **Actions** → Ваш коммит
2. Кликните на job **verter-check**
3. Разверните раздел **Run Verter Auto-Tests**
4. Увидите полный вывод с деталями

### Локально:

```bash
./test_runner.sh

# Полный лог сохраняется в:
cat test_results/test_results_*.log
```

---

## 📞 Нужна помощь?

- Проверьте `tasks/` для описания задания
- Посмотрите `answers/` для примера (но не копируйте!)
- Запустите `./test_runner.sh` локально для диагностики
- Читайте сообщения об ошибках внимательно

---

## 🎓 Пример полного workflow

```bash
# 1. Прочитать задание
cat tasks/first_word.txt

# 2. Создать решение
nano submissions/level_1/first_word.c

# 3. Локальная проверка
./test_runner.sh

# 4. Если тесты прошли:
git add submissions/level_1/first_word.c
git commit -m "Add first_word solution"
git push origin develop

# 5. Проверить результаты на GitHub Actions
```

---

## 🏆 Удачи на экзаменах!

Помните:
- Практика - ключ к успеху
- Учитесь на ошибках
- Не сдавайтесь!

**May the code be with you! 🚀**
