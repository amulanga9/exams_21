# School 21 Verter-like Test System

Автоматическая система тестирования для всех заданий School 21 C (экзамен Rank 02).

## 🚀 Быстрый старт

```bash
# Запустить все тесты
./test_runner.sh

# Результаты будут сохранены в папке test_results/
```

## 📁 Структура проекта

```
exams_21/
├── test_runner.sh          # Основной скрипт для запуска всех тестов
├── tests/                  # Директория с тестами (56 тестовых файлов)
│   ├── first_word_test.sh
│   ├── fizzbuzz_test.sh
│   ├── ft_strlen_test.sh
│   └── ...
├── answers/                # Решения заданий (56 .c файлов)
├── build/                  # Директория для компиляции (создается автоматически)
└── test_results/           # Логи результатов тестирования
```

## 🧪 Покрытие тестами

### Level 1 (12 заданий)
- first_word, fizzbuzz, ft_strcpy, ft_strlen
- ft_swap, ft_putstr, repeat_alpha, rev_print
- rot_13, rotone, search_and_replace, ulstr

### Level 2 (19 заданий)
- alpha_mirror, do_op, ft_atoi, ft_strcmp
- ft_strcspn, ft_strdup, ft_strpbrk, ft_strrev
- ft_strspn, inter, is_power_of_2, last_word
- max, print_bits, reverse_bits, snake_to_camel
- swap_bits, union, wdmatch

### Level 3 (15 заданий)
- add_prime_sum, epur_str, expand_str, ft_atoi_base
- ft_list_size, ft_range, ft_rrange, hidenp
- lcm, paramsum, pgcd, print_hex
- rstr_capitalizer, str_capitalizer, tab_mult

### Level 4 (10 заданий)
- flood_fill, fprime, ft_itoa, ft_list_foreach
- ft_list_remove_if, ft_split, rev_wstr, rostring
- sort_int_tab, sort_list

**Всего: 56 тестов**

## 🎯 Возможности системы

- ✅ Автоматическая компиляция с флагами `-Wall -Wextra -Werror`
- ✅ Тестирование как отдельных функций, так и полных программ
- ✅ Проверка корректности вывода
- ✅ Сравнение с стандартными функциями (где применимо)
- ✅ Цветной вывод результатов
- ✅ Детальные логи ошибок
- ✅ Сводная статистика по всем тестам

## 📊 Интерпретация результатов

После запуска `./test_runner.sh` вы увидите:

```
=================================
  School 21 Verter Test Runner
=================================

=== Testing Level 1 Tasks ===
Testing: first_word
✓ PASSED

Testing: fizzbuzz
✓ PASSED

...

=================================
         Test Summary
=================================
Total tests:  56
Passed:       41
Failed:       15
Success rate: 73%
```

## 🔧 Запуск отдельных тестов

Вы можете запускать тесты для отдельных заданий:

```bash
# Запустить тест для конкретного задания
./tests/first_word_test.sh
./tests/ft_strlen_test.sh
./tests/fprime_test.sh
```

## 📝 Формат тестов

Каждый тест:
1. Компилирует решение из папки `answers/`
2. Запускает несколько тестовых случаев
3. Сравнивает полученный результат с ожидаемым
4. Возвращает код выхода: 0 (успех) или 1 (ошибка)

## 🐛 Отладка

Если тест не прошел, проверьте:

1. **Ошибка компиляции**:
   - Проверьте код на соответствие требованиям компилятора
   - Убедитесь, что используются правильные заголовочные файлы

2. **Неправильный вывод**:
   - Сравните ожидаемый и полученный результат в логе
   - Проверьте логику программы

3. **Логические ошибки**:
   - Используйте отладчик (gdb)
   - Добавьте printf для отладки

## 🔍 Логи

Все результаты сохраняются в `test_results/` с временной меткой:
```
test_results/test_results_YYYYMMDD_HHMMSS.log
```

## 💡 Примеры использования

```bash
# Запустить все тесты и сохранить вывод
./test_runner.sh 2>&1 | tee my_results.txt

# Запустить все тесты и показать только ошибки
./test_runner.sh 2>&1 | grep -A 5 "FAILED"

# Подсчитать успешные тесты
./test_runner.sh 2>&1 | grep -c "PASSED"
```

## 🤝 Вклад в проект

Система тестирования была создана для помощи студентам School 21 в подготовке к экзаменам.

Если вы нашли ошибку в тестах или хотите добавить новые тест-кейсы:
1. Создайте новый тестовый скрипт в папке `tests/`
2. Следуйте существующему формату тестов
3. Убедитесь, что скрипт исполняемый (`chmod +x`)

## 📚 Дополнительная информация

- Все тесты используют стандартную библиотеку C
- Компиляция выполняется с флагами: `-Wall -Wextra -Werror`
- Временные файлы создаются в папке `build/`
- Система совместима с norminette (стандарт кода School 21)

## 🎓 Для студентов School 21

Эта система максимально приближена к работе Verter - автоматической системы проверки School 21.

**Рекомендации:**
- Запускайте тесты перед отправкой решений
- Изучайте ошибки и исправляйте их
- Добавляйте свои тестовые случаи для лучшего понимания
- Помните: тесты проверяют основные случаи, но не гарантируют 100% правильность

---

**Удачи на экзаменах! 🚀**
