# 📋 ШПАРГАЛКА ПО C - БЫСТРЫЙ ДОСТУП

> Краткая выжимка из МЕГА-ГАЙДА для быстрого повторения

---

## 🚀 Базовая структура программы

```c
#include <stdio.h>

int main(void) {
    // Твой код здесь

    return 0;  // Успешное завершение
}
```

---

## 📦 Типы данных

| Тип | Размер | Диапазон | Формат scanf | Формат printf |
|-----|--------|----------|--------------|---------------|
| `int` | 4 байта | -2,147,483,648 ... 2,147,483,647 | `%d` | `%d` |
| `float` | 4 байта | ±3.4e-38 ... ±3.4e+38 | `%f` | `%f` |
| `double` | 8 байт | ±1.7e-308 ... ±1.7e+308 | `%lf` | `%lf` или `%f` |
| `char` | 1 байт | -128 ... 127 | `%c` | `%c` |

---

## 💬 Ввод/Вывод

### printf - вывод

```c
printf("Простой текст\n");
printf("Число: %d\n", 42);
printf("Дробное: %.2f\n", 3.14);  // 2 знака после запятой
printf("Несколько: %d и %f\n", 42, 3.14);
```

### scanf - ввод

```c
int age;
scanf("%d", &age);  // ⚠️ НЕ ЗАБЫВАЙ &!

float price;
scanf("%f", &price);

int a, b;
scanf("%d %d", &a, &b);  // Два числа через пробел
```

### Проверка scanf

```c
int result = scanf("%d", &number);
if (result != 1) {
    printf("Ошибка ввода!\n");
}
```

---

## 🔢 Операторы

### Арифметические
```c
5 + 3   // 8 (сложение)
5 - 3   // 2 (вычитание)
5 * 3   // 15 (умножение)
10 / 3  // 3 (целочисленное деление!)
10 % 3  // 1 (остаток)
```

### Сравнения
```c
==  // равно
!=  // не равно
>   // больше
<   // меньше
>=  // больше или равно
<=  // меньше или равно
```

### Логические
```c
&&  // И (AND)
||  // ИЛИ (OR)
!   // НЕ (NOT)

// Пример:
if (x > 0 && x < 10) { ... }  // x между 0 и 10
```

---

## 🎯 Условия

### if-else

```c
if (условие) {
    // выполнится если true
} else {
    // выполнится если false
}
```

### if-else if-else

```c
if (x > 0) {
    printf("Положительное\n");
} else if (x < 0) {
    printf("Отрицательное\n");
} else {
    printf("Ноль\n");
}
```

---

## 🔄 Циклы

### while

```c
int i = 0;
while (i < 5) {
    printf("%d\n", i);
    i++;
}
```

### for

```c
for (int i = 0; i < 5; i++) {
    printf("%d\n", i);
}
```

### do-while

```c
int i = 0;
do {
    printf("%d\n", i);
    i++;
} while (i < 5);
```

---

## 📝 Паттерны для School 21

### Шаблон с проверкой ввода (2 числа)

```c
#include <stdio.h>

int main(void) {
    int a, b;
    int cnt;
    int lastchar;

    cnt = scanf("%d %d", &a, &b);
    lastchar = getchar();

    if (cnt != 2 || lastchar != 0x0a) {
        printf("n/a");
        return 0;
    }

    // Твоя логика здесь
    printf("%d", a * b);

    return 0;
}
```

### Шаблон с проверкой ввода (1 число)

```c
#include <stdio.h>

int main(void) {
    int x;
    int cnt;
    int lastchar;

    cnt = scanf("%d", &x);
    lastchar = getchar();

    if (cnt != 1 || lastchar != 0x0a) {
        printf("n/a");
        return 0;
    }

    // Твоя логика
    printf("%d", x * 2);

    return 0;
}
```

---

## 🔍 Частые ошибки и как их избежать

### ❌ Ошибка 1: Забыл & в scanf
```c
int x;
scanf("%d", x);    // ❌ НЕПРАВИЛЬНО!
scanf("%d", &x);   // ✅ ПРАВИЛЬНО!
```

### ❌ Ошибка 2: Путаница = и ==
```c
if (x = 5) { ... }   // ❌ Присваивание!
if (x == 5) { ... }  // ✅ Сравнение!
```

### ❌ Ошибка 3: Целочисленное деление
```c
int result = 10 / 3;        // result = 3 ❌
float result = 10.0 / 3.0;  // result = 3.333 ✅
```

### ❌ Ошибка 4: Неправильный формат
```c
float f;
scanf("%lf", &f);   // ❌ %lf для double, не float!
scanf("%f", &f);    // ✅ ПРАВИЛЬНО!

double d;
scanf("%f", &d);    // ❌ %f для float, не double!
scanf("%lf", &d);   // ✅ ПРАВИЛЬНО!
```

### ❌ Ошибка 5: Забыл \n в printf
```c
printf("Hello");      // Может не вывестись сразу!
printf("Hello\n");    // ✅ Сразу выведется
```

---

## 🎮 ASCII коды (часто используемые)

| Символ | Dec | Hex | Описание |
|--------|-----|-----|----------|
| `\n` | 10 | 0x0a | Новая строка (Enter) |
| ` ` (space) | 32 | 0x20 | Пробел |
| `0` | 48 | 0x30 | Цифра 0 |
| `9` | 57 | 0x39 | Цифра 9 |
| `A` | 65 | 0x41 | Заглавная A |
| `Z` | 90 | 0x5a | Заглавная Z |
| `a` | 97 | 0x61 | Строчная a |
| `z` | 122 | 0x7a | Строчная z |

---

## 📐 Полезные функции <math.h>

```c
#include <math.h>

pow(x, y)     // x в степени y
sqrt(x)       // Квадратный корень
abs(x)        // Модуль (абсолютное значение)
ceil(x)       // Округление вверх
floor(x)      // Округление вниз
```

Компиляция с math.h:
```bash
clang program.c -o program -lm
```

---

## 🛠️ Компиляция

### Базовая
```bash
clang program.c -o program
./program
```

### School 21 стандарт
```bash
clang -Wall -Wextra -Werror program.c -o program
```

Флаги:
- `-Wall` - все предупреждения
- `-Wextra` - дополнительные предупреждения
- `-Werror` - предупреждения = ошибки

---

## 🧪 Тестирование

### Тестовые случаи для любой программы:

1. ✅ **Обычный ввод**: `5 10`
2. ✅ **Ноль**: `0 0`
3. ✅ **Отрицательные**: `-5 -10`
4. ✅ **Большие числа**: `999999 888888`
5. ❌ **Некорректный ввод**: `abc`
6. ❌ **Неполный ввод**: `5` (если ожидалось 2)
7. ❌ **Лишний ввод**: `5 10 extra`

---

## 💡 Быстрые советы

### DO ✅
- Проверяй возвращаемое значение scanf
- Используй осмысленные имена переменных
- Добавляй комментарии
- Тестируй на разных данных
- Компилируй с флагами `-Wall -Wextra -Werror`

### DON'T ❌
- Не забывай `&` в scanf
- Не путай `=` и `==`
- Не игнорируй предупреждения компилятора
- Не копипасть код без понимания
- Не сдавайся при первой ошибке!

---

## 🎯 Алгоритм решения любой задачи

```
1. ПРОЧИТАЙ условие 3 раза
   └─ Что на входе? Что на выходе?

2. НАПИШИ примеры вручную
   └─ Ввод: 5 10 → Вывод: 50

3. РАСПИШИ алгоритм словами
   └─ "Считать два числа, умножить, вывести"

4. НАПИШИ код
   └─ Сначала структура, потом детали

5. ПРОТЕСТИРУЙ на разных данных
   └─ Нормальные, граничные, некорректные

6. ИСПРАВЬ ошибки
   └─ Компилятор - твой друг!

7. ПРОВЕРЬ NORMINETTE
   └─ norminette program.c

8. СДАЙ РЕШЕНИЕ
   └─ git add, commit, push
```

---

## 📚 Быстрые ссылки

### Документация
- [cppreference.com](https://en.cppreference.com/w/c) - полная документация
- [Metanit C](https://metanit.com/cpp/c/) - учебник на русском

### Видео (начни с этих)
- [Хауди Хо - Основы C](https://www.youtube.com/watch?v=LqKq0O1hJ0Y&list=PLtMlNhVkUDCNxCkd5NHjJQCPqLFxFKw8y)
- [CS50 - Гарвард](https://www.youtube.com/watch?v=8mAITcNt710)

### Практика
- [Codewars](https://www.codewars.com/)
- [HackerRank](https://www.hackerrank.com/)

---

## 🆘 Что делать если застрял?

1. **Перечитай условие** - может, что-то упустил
2. **Распечатай переменные** - добавь `printf` для отладки
3. **Разбей на части** - решай по кусочкам
4. **Погугли ошибку** - часто есть решение
5. **Спроси товарища** - объяснение вслух помогает
6. **Отдохни 15 минут** - мозгу нужна перезагрузка
7. **Начни заново** - иногда проще переписать

---

## 🏆 Мантра программиста

```
while (не_получается) {
    пробуй_снова();
    учись_на_ошибках();
}

success = true;
```

**Помни:** Каждая ошибка делает тебя сильнее! 💪

---

**Версия:** 1.0
**Дата:** 2025-11-17
**Для:** School 21 Students 🎓

**P.S.** Распечатай эту шпаргалку и держи под рукой на экзамене! 📄
