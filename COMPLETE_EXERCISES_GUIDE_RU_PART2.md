# 🎓 ПОЛНЫЙ РАЗБОР - ЧАСТЬ 2

## Продолжение: Ex_04 - Ex_05

---

### 📝 Ex_04_04: Наибольшая цифра в числе

#### Код

```c
#include <stdio.h>

int main(void) {
    long n;
    int cnt, lastchar, max = 0;

    cnt = scanf("%ld", &n);
    lastchar = getchar();

    if (cnt != 1 || lastchar != 0x0a) {
        printf("n/a");
        return 0;
    }

    n = (n < 0) ? -n : n;  // Сделать положительным

    while (n > 0) {
        if (n % 10 > max)
            max = n % 10;
        n = n / 10;
    }

    printf("%d", max);
}
```

#### 🎯 Алгоритм

```
1. Считать число
2. Сделать его положительным (если отрицательное)
3. max = 0
4. ПОКА число > 0:
   5. Взять последнюю цифру (n % 10)
   6. ЕСЛИ она > max: обновить max
   7. Отрезать последнюю цифру (n / 10)
8. Вывести max
```

#### 📊 Пример работы

**Число:** 71283

| Итерация | n | n % 10 | max | Действие |
|----------|---|--------|-----|----------|
| 1 | 71283 | 3 | 0 | 3 > 0 → max = 3 |
| 2 | 7128 | 8 | 3 | 8 > 3 → max = 8 |
| 3 | 712 | 2 | 8 | 2 < 8 → без изменений |
| 4 | 71 | 1 | 8 | 1 < 8 → без изменений |
| 5 | 7 | 7 | 8 | 7 < 8 → без изменений |
| 6 | 0 | - | 8 | Конец! Вывод: 8 |

#### 🆕 Тернарный оператор

```c
n = (n < 0) ? -n : n;
```

**Читается:** n = (условие) ? значение_если_true : значение_если_false

**Аналог с if-else:**
```c
if (n < 0) {
    n = -n;
} else {
    n = n;
}
```

**Примеры:**
```c
int max = (a > b) ? a : b;           // Максимум из двух
int abs = (x < 0) ? -x : x;          // Модуль числа
char grade = (score >= 60) ? 'P' : 'F';  // Pass/Fail
```

#### 🎮 Аналогия

Ищешь самую высокую башню в городе:
1. Идёшь по улице (перебираешь цифры)
2. Смотришь на каждую башню (цифру)
3. Запоминаешь самую высокую
4. В конце знаешь максимум!

#### 🎥 Видео по теме

1. [Извлечение цифр из числа](https://www.youtube.com/watch?v=3wqWEX8CQY0)
2. [Тернарный оператор](https://www.youtube.com/watch?v=sDgePRLv-yY)

---

### 📝 Ex_04_05: Произведение нечётных цифр

#### Код

```c
#include <stdio.h>
#include <math.h>

int main(void) {
    long n;
    int cnt, lastchar, s = 0;

    cnt = scanf("%ld", &n);
    lastchar = getchar();

    if (cnt != 1 || lastchar != 0x0a) {
        printf("n/a");
        return 0;
    }

    n = fabs(n);  // Модуль

    while (n > 0) {
        if ((n % 10) % 2 != 0) {  // Если цифра нечётная
            s *= (n % 10);
        }
        n = n / 10;
    }

    printf("%d", s);
}
```

#### 🎯 Как проверить чётность?

```c
число % 2 == 0  // Чётное
число % 2 != 0  // Нечётное
```

**Примеры:**
```c
4 % 2 = 0  // Чётное
5 % 2 = 1  // Нечётное
8 % 2 = 0  // Чётное
9 % 2 = 1  // Нечётное
```

#### 📊 Пример: число 2351

| Итерация | n | n % 10 | Чётная? | s | Действие |
|----------|---|--------|---------|---|----------|
| Начало | 2351 | - | - | 0 | s = 0 |
| 1 | 2351 | 1 | Нет | 0 | s = 0 * 1 = 0 ❌ БАГ! |
| 2 | 235 | 5 | Нет | 0 | s = 0 * 5 = 0 |
| 3 | 23 | 3 | Нет | 0 | s = 0 * 3 = 0 |
| 4 | 2 | 2 | Да | 0 | Пропускаем |

**Проблема:** Начальное значение `s = 0`! Умножение на 0 даёт 0!

**Правильное решение:**
```c
int s = 1;  // Начинаем с 1, не 0!
```

#### 📊 Правильный пример: число 2351

| Итерация | n | n % 10 | Чётная? | s | Действие |
|----------|---|--------|---------|---|----------|
| Начало | 2351 | - | - | 1 | s = 1 ✅ |
| 1 | 2351 | 1 | Нет | 1 | s = 1 * 1 = 1 |
| 2 | 235 | 5 | Нет | 1 | s = 1 * 5 = 5 |
| 3 | 23 | 3 | Нет | 5 | s = 5 * 3 = 15 |
| 4 | 2 | 2 | Да | 15 | Пропускаем |
| Результат | - | - | - | **15** | 1 × 5 × 3 = 15 ✅ |

#### 🎮 Аналогия

Считаешь произведение только красных шаров:
- Синий шар (чётная цифра) → пропускаем
- Красный шар (нечётная цифра) → умножаем

Начинаем с 1 (нейтральный элемент умножения)!

#### 🎥 Видео по теме

1. [Чётные и нечётные числа](https://www.youtube.com/watch?v=N2J0iOHLvL0)
2. [Операции с цифрами](https://www.youtube.com/watch?v=3wqWEX8CQY0)

---

## Ex_05: Динамическая память и массивы (5 заданий)

### 🎯 Что ты узнаешь:
- malloc() - выделение памяти
- free() - освобождение памяти
- Динамические массивы
- realloc() - изменение размера
- Скалярное произведение векторов
- Работа с массивами переменной длины

---

### 📝 Ex_05_01: Скалярное произведение векторов

#### Код (упрощённая версия)

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int N, *A, *B, result = 0;
    char c;

    // Считываем размер
    if (scanf("%d%c", &N, &c) != 2 || c != '\n' || N < 1) {
        printf("n/a");
        return 1;
    }

    // Выделяем память
    A = malloc(N * sizeof(int));
    B = malloc(N * sizeof(int));

    if (!A || !B) {
        printf("n/a");
        free(A);
        free(B);
        return 1;
    }

    // Читаем массив A
    for (int i = 0; i < N; i++) {
        if (scanf("%d", &A[i]) != 1) {
            printf("n/a");
            free(A);
            free(B);
            return 1;
        }
    }

    // Читаем массив B
    for (int i = 0; i < N; i++) {
        if (scanf("%d", &B[i]) != 1) {
            printf("n/a");
            free(A);
            free(B);
            return 1;
        }
    }

    // Считаем скалярное произведение
    for (int i = 0; i < N; i++) {
        result += A[i] * B[i];
    }

    printf("%d\n", result);

    // Освобождаем память
    free(A);
    free(B);

    return 0;
}
```

#### 🆕 Динамическая память

**Зачем нужна?**
Размер массива неизвестен заранее! Пользователь вводит N - и только потом мы создаём массив.

**Обычный массив (статический):**
```c
int arr[100];  // Размер известен при компиляции
```

**Динамический массив:**
```c
int *arr = malloc(N * sizeof(int));  // Размер известен во время выполнения
```

#### 🧩 Функции работы с памятью

**1. malloc() - выделить память**
```c
int *ptr = malloc(размер_в_байтах);
```

Пример:
```c
int *arr = malloc(10 * sizeof(int));  // Массив из 10 int
double *darr = malloc(5 * sizeof(double));  // 5 double
char *str = malloc(100 * sizeof(char));  // Строка 100 символов
```

**2. free() - освободить память**
```c
free(ptr);
```

**КРИТИЧНО!** Всегда освобождай память после использования!

**3. Проверка успешности**
```c
int *ptr = malloc(size);
if (ptr == NULL) {  // или просто if (!ptr)
    printf("Ошибка выделения памяти!");
    return 1;
}
```

#### 📐 Скалярное произведение

**Формула:**
```
A · B = A[0]*B[0] + A[1]*B[1] + ... + A[N-1]*B[N-1]
```

**Пример:**
```
A = [1, 2, 3]
B = [4, 5, 6]

A · B = 1*4 + 2*5 + 3*6 = 4 + 10 + 18 = 32
```

#### 📊 Визуализация

```
Массив A:  [1]  [2]  [3]
           ↓    ↓    ↓   (умножаем)
Массив B:  [4]  [5]  [6]
           ↓    ↓    ↓
Результат:  4 + 10 + 18 = 32
```

#### ⚠️ Правила работы с памятью

1. **Всегда проверяй malloc!**
```c
ptr = malloc(size);
if (!ptr) {
    // Обработка ошибки
}
```

2. **Всегда вызывай free!**
```c
free(A);
free(B);
```

3. **Не используй после free!**
```c
free(ptr);
printf("%d", ptr[0]);  // ❌ ОШИБКА! Использование освобождённой памяти
```

4. **Не забывай free даже при ошибках!**
```c
if (error) {
    free(A);  // ✅ Освобождаем перед выходом
    free(B);
    return 1;
}
```

#### 🎮 Аналогия

**malloc** - это аренда квартиры:
- Платишь за размер (количество комнат)
- Получаешь ключи (указатель)
- Живёшь, пока нужно
- **free** - возвращаешь ключи когда уезжаешь

Если не вернёшь ключи (не вызовешь free) - **memory leak** (утечка памяти)!

#### 🎥 Видео по теме

1. [malloc и free в C](https://www.youtube.com/watch?v=lQP4X3JeQ_U)
2. [Динамическая память - полный гайд](https://www.youtube.com/watch?v=R0qIYWo8igs)
3. [Скалярное произведение векторов](https://www.youtube.com/watch?v=WNuIhXo39_k)
4. [Утечки памяти и как их избежать](https://www.youtube.com/watch?v=fXNhEzbJuyo)

---

### 📝 Ex_05_03: Умножение элементов на n-1, n-2...

#### Концепция

Дан массив. Нужно умножить:
- 1-й элемент на (n-1)
- 2-й элемент на (n-2)
- 3-й элемент на (n-3)
- ...
- Последний на 0

#### Пример

**Входной массив:** [2, 3, 4, 5]
**n = 4**

```
2 * (4-1) = 2 * 3 = 6
3 * (4-2) = 3 * 2 = 6
4 * (4-3) = 4 * 1 = 4
5 * (4-4) = 5 * 0 = 0

Результат: [6, 6, 4, 0]
```

#### Код (упрощённый)

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n, *arr;

    scanf("%d", &n);
    arr = malloc(n * sizeof(int));

    if (!arr) {
        printf("n/a");
        return 1;
    }

    // Читаем массив
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Умножаем каждый элемент
    for (int i = 0; i < n; i++) {
        arr[i] = arr[i] * (n - 1 - i);
    }

    // Выводим результат
    for (int i = 0; i < n; i++) {
        printf("%d", arr[i]);
        if (i < n - 1) printf(" ");
    }

    free(arr);
    return 0;
}
```

#### 🎮 Аналогия

У тебя есть N коробок с подарками:
- 1-я коробка × (N-1) друзей
- 2-я коробка × (N-2) друзей
- ...
- Последняя × 0 друзей (никому не досталось!)

#### 🎥 Видео по теме

1. [Массивы в C](https://www.youtube.com/watch?v=7mwgA2IJBEU)
2. [Алгоритмы с массивами](https://www.youtube.com/watch?v=pBWp-woNRAY)

---

### 📝 Ex_05_04: Фильтрация массива (числа 0-9)

#### Концепция

Считывать числа пока не введён -1. Вывести только те, которые попадают в диапазон 0-9.

#### Пример

**Ввод:** 5 12 7 -3 2 9 100 -1
**Вывод:** 5 7 2 9

#### Алгоритм

```
1. Создать пустой массив
2. ПОКА не введено -1:
   3. Считать число
   4. Добавить в массив
   5. Увеличить размер массива (realloc)
6. Вывести только числа от 0 до 9
```

#### 🆕 realloc() - изменить размер памяти

```c
ptr = realloc(ptr, новый_размер);
```

**Примеры:**
```c
int *arr = malloc(10 * sizeof(int));  // 10 элементов
arr = realloc(arr, 20 * sizeof(int));  // Теперь 20!
arr = realloc(arr, 5 * sizeof(int));   // Уменьшили до 5
```

**⚠️ Важно:** realloc может вернуть NULL!

```c
int *new_arr = realloc(arr, new_size);
if (!new_arr) {
    // Ошибка! Но arr ещё валиден!
    free(arr);
    return 1;
}
arr = new_arr;  // Обновляем только если успех
```

#### 🎮 Аналогия

**realloc** - это переезд в квартиру побольше:
- Была студия (10 элементов)
- Переехал в двушку (20 элементов)
- realloc сам перенёс все вещи!

Но иногда не хватает мест в доме → вернёт NULL!

#### 🎥 Видео по теме

1. [realloc подробно](https://www.youtube.com/watch?v=PSkZLgb5gQs)
2. [Динамические массивы](https://www.youtube.com/watch?v=OYCxR0x7uFg)

---

### 📝 Ex_05_05: Реверс массива

#### Концепция

Считать массив чисел и вывести в обратном порядке.

#### Пример

**Ввод:** 1 2 3 4 5
**Вывод:** 5 4 3 2 1

#### Алгоритм

```
1. Считать массив
2. Вывести с конца:
   FOR i от (N-1) до 0:
       Вывести arr[i]
```

#### Код (основная идея)

```c
// Считали массив arr размером n

// Вывод в обратном порядке
for (int i = n - 1; i >= 0; i--) {
    printf("%d", arr[i]);
    if (i > 0) printf(" ");
}
```

#### 🎮 Аналогия

Читаешь книгу с конца:
- Последняя страница → первая
- Предпоследняя → вторая
- ...
- Первая → последняя

#### 🎥 Видео по теме

1. [Реверс массива](https://www.youtube.com/watch?v=oUfUdxcXXPE)
2. [Алгоритмы реверсирования](https://www.youtube.com/watch?v=M37sNf5FMcQ)

---

## 📊 Сравнительная таблица Ex_05

| Упражнение | Тема | Сложность | Ключевая техника |
|------------|------|-----------|------------------|
| Ex_05_01 | Скалярное произведение | ⭐⭐ | malloc, free, два массива |
| Ex_05_03 | Умножение на счётчик | ⭐⭐ | Динамический массив, цикл |
| Ex_05_04 | Фильтрация | ⭐⭐⭐ | realloc, условия |
| Ex_05_05 | Реверс | ⭐⭐ | Обратный цикл |

---

## 💡 Ключевые концепции Ex_05

### ✅ Что обязательно нужно знать:

1. **malloc/free** - основа динамической памяти
2. **Проверка на NULL** - всегда после malloc!
3. **Освобождение памяти** - даже при ошибках
4. **realloc** - изменение размера массива
5. **Индексация массивов** - от 0 до n-1

### ⚠️ Частые ошибки:

1. Забыть free → утечка памяти
2. Не проверить malloc → сегфолт
3. Использовать после free → undefined behavior
4. Неправильный размер в malloc → переполнение
5. Забыть умножить на sizeof

### 🎯 Чеклист для malloc:

```
□ Выделил память (malloc)
□ Проверил на NULL
□ Использовал память
□ Освободил память (free)
□ Не использую после free
```

---

**Продолжение следует в Части 3: Ex_06-08 (Матрицы, строки, структуры)**

---

**Версия:** 1.0 (Часть 2 из 3)
**Дата:** 2025-11-17
**Прогресс:** 24/34 упражнений (71%)
